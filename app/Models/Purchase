<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Purchase extends Model
{
    use HasFactory;

    // Define your table, fillable fields, etc. if necessary
    protected $table = 'purchases'; // if the table name is not plural
    // protected $fillable = [...]; // define fillable attributes if needed
}
